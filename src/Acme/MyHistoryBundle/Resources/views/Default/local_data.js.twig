var timeline_data = {  // save as a global variable
'dateTimeFormat': 'iso8601',
'wikiURL': "http://simile.mit.edu/shelf/",
'wikiSection': "Simile Cubism Timeline",

'events' : [

{% for item in event %}

        {'start': '{{item.start|date("c")}}',
		'end': '{{item.end|date("c")}}',
        'title': '{{item.title}}',
        'description': '{{item.description}}',
        'link': '{{item.link}}',
		'durationEvent':{% if not item.durationEvent %} false {% else %} true {% endif %}
        },

{% endfor %}
]
}